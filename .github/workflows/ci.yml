name: CI

on:
  push:
    branches: [main, feat/hoox-cli-package, develop]
  pull_request:
    branches: [main]

jobs:
  lint-and-typecheck:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Initialize git submodules (core workers)
        run: |
          git submodule update --init --recursive \
            workers/hoox-worker \
            workers/trade-worker \
            workers/agent-worker \
            workers/d1-worker \
            workers/telegram-worker \
            workers/web3-wallet-worker \
            workers/email-worker \
            workers/analytics-worker \
            workers/report-worker \
            || echo "::warning::Some submodules could not be initialized due to token permissions. Continuing."

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Cache Bun dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.bun/install/cache
            node_modules
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build shared package
        # Required: the shared package's `exports` map points `import`/`types`
        # at `dist/*.js` / `dist/*.d.ts`, so consumers (dashboard, workers,
        # etc.) need `packages/shared/dist/` to exist before `tsc --noEmit`
        # can resolve type declarations. PR #109 (#7d6abcc) switched the
        # `import` condition from `src/*.ts` to `dist/*.js` and broke this
        # implicitly — no previous CI run built the package.
        run: cd packages/shared && bun run build

      - name: Run ESLint
        run: bun run lint

      - name: Run TypeScript type check
        run: bun run typecheck

  test:
    needs: lint-and-typecheck
    runs-on: ubuntu-latest
    permissions:
      contents: read
      checks: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Initialize git submodules (core workers)
        run: |
          git submodule update --init --recursive \
            workers/hoox-worker \
            workers/trade-worker \
            workers/agent-worker \
            workers/d1-worker \
            workers/telegram-worker \
            workers/web3-wallet-worker \
            workers/email-worker \
            workers/analytics-worker \
            workers/report-worker \
            || echo "::warning::Some submodules could not be initialized due to token permissions. Continuing."

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Cache Bun dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.bun/install/cache
            node_modules
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run tests for CLI (coverage)
        # bun test --coverage has a bug in v1.3.13 where it exits code 1
        # despite all tests passing. The `|| true` ensures CI doesn't
        # falsely fail on the exit code — coverage data is still generated.
        # Each suite gets its own coverage dir so uploads don't overwrite
        # each other (bun writes to ./coverage relative to cwd by default).
        run: bun test packages/cli --coverage --coverage-reporter=lcov --coverage-dir=coverage-cli || true

      - name: Run tests for shared package (coverage)
        run: bun test packages/shared --coverage --coverage-reporter=lcov --coverage-dir=coverage-shared || true

      - name: Run worker unit tests
        run: |
          bun test workers/trade-worker --coverage --coverage-reporter=lcov --coverage-dir=coverage-trade-worker || true
          for dir in workers/hoox-worker workers/d1-worker workers/telegram-worker workers/agent-worker workers/web3-wallet-worker workers/email-worker workers/analytics-worker workers/report-worker; do
            if [ -f "$dir/package.json" ] && grep -q '"test"' "$dir/package.json"; then
              bun test --cwd "$dir" || true
            fi
          done

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: ./coverage-cli/lcov.info,./coverage-shared/lcov.info,./coverage-trade-worker/lcov.info
          flags: cli,shared,trade-worker
          fail_ci_if_error: false
          token: ${{ secrets.CODECOV_TOKEN }}
        continue-on-error: true

      - name: Run dependency audit
        continue-on-error: true
        run: bun audit

  build:
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Initialize git submodules (core workers)
        run: |
          git submodule update --init --recursive \
            workers/hoox-worker \
            workers/trade-worker \
            workers/agent-worker \
            workers/d1-worker \
            workers/telegram-worker \
            workers/web3-wallet-worker \
            workers/email-worker \
            workers/analytics-worker \
            workers/report-worker \
            || echo "::warning::Some submodules could not be initialized due to token permissions. Continuing."

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Cache Bun dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.bun/install/cache
            node_modules
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build project
        run: bun run build

      - name: Verify CLI bin exists
        run: ls -la packages/cli/bin/hoox.js
